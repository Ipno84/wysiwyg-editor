import type { Team } from '@/editor/state/editor/typings';

const teams: Team[] = [
    {
        id: 'defaultTeam',
        name: 'Default Team',
        description: 'Default Team description',
    },
];

export { teams };
