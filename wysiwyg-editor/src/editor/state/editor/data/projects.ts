import type { Project } from '@/editor/state/editor/typings';

const projects: Project[] = [{ id: 'defaultProject', name: 'Default Project', description: 'Default project for the editor' }];

export { projects };
