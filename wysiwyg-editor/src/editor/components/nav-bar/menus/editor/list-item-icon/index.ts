import MuiListItemIcon from "@mui/material/ListItemIcon/ListItemIcon";
import { styled } from "@mui/material/styles";

const ListItemIcon = styled(MuiListItemIcon)({ minWidth: 38 });

export { ListItemIcon };
